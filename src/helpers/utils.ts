export const startLoader = () => {
  return;
};

export const stopLoader = () => {
  return;
};

export const showToaster = (message: string, type: "S" | "E", title?: string) => {
  return { message, type, title };
};
